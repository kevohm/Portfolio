import styled from "styled-components"
import tw from "twin.macro"

export const Main = styled.div`
  ${tw`h-[100px] pl-12 flex items-center justify-start text-2xl dark:text-white`}
  font-weight:600;
`;